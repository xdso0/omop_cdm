pandas>=2.0
numpy>=1.24
pyreadstat>=1.2      # sas7bdat 읽기/쓰기 (SAS 원천 사용 시)
openpyxl>=3.1        # .xlsx 읽기/쓰기
pyyaml>=6.0          # 파이프라인 설정 로딩
pyarrow>=14.0        # parquet 산출(선택)
SQLAlchemy>=2.0      # DB 입출력
psycopg2-binary>=2.9 # PostgreSQL 드라이버 (다른 DB는 해당 드라이버로 교체)
